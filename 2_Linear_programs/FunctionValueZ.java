public class FunctionValueZ {
    //Задача 1. Найдите  значение функции: z = ( (a – 3 ) * b / 2) + c.
    public static void main(String[] args) {
	double a = 2;
	double b = 3;
	double c = 4;
	double z = ((a - 3) * b / 2) + c;
        System.out.println("z = " + z);
    }
}
