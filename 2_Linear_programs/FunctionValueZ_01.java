public class FunctionValueZ_01 {
    //Задача 1. Найдите  значение функции: z = ( (a – 3 ) * b / 2) + c.
    public static void main(String[] args) {
        double numA = 2;
        double numB = 3;
        double numC = 4;
        double result = ((numA - 3) * numB / 2) + numC;
        System.out.println("Значение функции z = " + result);
    }
}
